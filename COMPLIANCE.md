# COMPLIANCE — AML Cycle 2 契约逐条对照（MemoryCore，文本记忆赛道 · 开源方法榜）

> 依据：规范副本 `/home/echo/D/aml-2026/AML-SPEC-cycle2-20260918.md`（2026-09-18 23:12 抓取版）。
> 每条给出：规范原文（引号内为原文摘句）→ 我们的实现 → 证据。提交前会再抓一次官网页面对比（站点是活 SPA）。

## A. 接入形态与端点

| # | 规范要求（摘句） | 我们的实现 | 证据 |
|---|---|---|---|
| A1 | "Open-source methods and commercial products both use participant-hosted Add/Search APIs. AML does not deploy repository-only submissions." | 自托管公网端点：`http://47.108.28.129:18000/add|/search|/health`（阿里云 frps → frpc → echopc 服务） | `TASK-capacity.md`/通道搭建记录；2026-09-19 晚同口径配对实测：Add 64 并发墙钟 **216.2 s**（E3 开启）/ **213.9 s**（E3 前对照），成功率均 100%；公网入口自检通过（Mac → 公网：`/health` 200、无密钥 401、带密钥 `/add` 200、`/search` 命中） |
| A2 | "URL 中不得包含用户名、密码等凭据，也不得指向私有、回环或链路本地地址" | 公网 IPv4 + 非标端口，URL 无凭据；鉴权在请求头 | 公网探针 `/health` 200、`/add` 200 |
| A3 | "生产环境建议使用 HTTPS"（建议非强制） | 当前 HTTP（IP 证书链路已评估，未采用）；如需 HTTPS 可切换 | 决策记录（Obsidian「配置变更」） |
| A4 | "If you provide a deployed endpoint, it will remain publicly reachable and stable for at least 30 days after submission." | 到期日设为 **2026-12-05**（覆盖最晚 10/31 提交 + 30 天） | 两边自失效脚本 + `systemctl list-timers` 显示 2026-12-05 12:00 |

## B. Add 接口

| # | 规范要求 | 我们的实现 | 证据 |
|---|---|---|---|
| B1 | "写入方式 同步；记忆写入完成后，Add 接口返回 HTTP 200" / "服务内部可以采用异步处理，但 Add 接口必须等待处理完成后再返回" | 全同步：写完（含向量）才返回 200 | 探针：Add 后立即 Search 命中（200/200，评审三组循环各 200 次） |
| B2 | "写入完成并且相关记忆能够立即检索后，才能返回成功响应" | 冻结口径：两点必须同时成立（**同一进程内可见**）；评审已复现"运行期重建数据目录"会破坏该保证 → P0.5 修复中（数据目录 identity guard） | `review-p0-impl-20260919/REVIEW.md` §3.2 + P0.5 任务书 |
| B3 | `request_id` "必填…成功响应必须原样返回该值" | 原样回显；幂等：同 request_id+同 body → 200 replay；不同 body → 409 | 我实测：replay 200 / 409；`working_memory` 仍 1 行 |
| B4 | `messages` "必填…每条消息包含 role 和非空 content"；文本用字符串或有序 text/image_url 内容数组 | 支持字符串与数组；role 限 `user`/`assistant`，非法 → 422 且零写入 | 3A 验证据；`tests/test_aml_cycle2_contract.py` |
| B5 | `timestamp` "可选，单位为 Unix 毫秒" | 接受并忽略（排序用持久化时间）——已在材料 §4.5 披露 | 规范第 132-133 行口径 + 材料披露节 |
| B6 | `user_id` "必填…写入和检索时必须保持一致" | 一对一映射到存储 `author_id`，写入/去重/检索全程携带；SQL 层过滤 | 跨 user_id 检索不到（测试覆盖） |
| B7 | `session_id` "必填…不作为 Search 的筛选条件" | 记录来源，不参与检索过滤 | 代码 + 测试 |
| B8 | "单次请求最长 30 分钟" | 单请求预算默认 600 s（可配），实测单次 Add 3.4~4.5 s；64 并发整批 **216.2 s**（配对对照 213.9 s；平台超时 1200 s → 约 5.6 倍余量） | 本机同口径配对实测（2026-09-19 晚） |
| B9 | "202 仅当…已审核绑定…Add 状态查询地址时支持；未绑定时返回 202 属于契约错误" | **不返回 202** | 探针 + 测试 |
| B10 | Add 响应 "success 必填，且必须是布尔值 true；request_id/user_id/session_id 全部必填并完全一致" | 完全符合 | 公网/本机探针响应体 |

## C. Search 接口

| # | 规范要求 | 我们的实现 | 证据 |
|---|---|---|---|
| C1 | `query` "必填…不得替换为最终答案或使用评测金标" | 原文检索；不使用金标、不生成答案 | 材料 §1 边界声明 |
| C2 | `options` "可选；选择题（含 Streaming）在 Search 顶层传入选项文本数组" | 支持；仅拼接入检索查询做兜底召回，不生成答案、不写记忆 | 3A/评审证据 |
| C3 | `user_id` "只能在该 user_id 对应的记忆范围内检索" | 同 B6；跨用户不可见 | 测试覆盖 |
| C4 | `top_k` "必填。返回的记忆数量不得超过该值；正式外部评测固定为 100" | 必填；缺失/非法 → 422；>100 clamp 到 100；**响应永不超过 top_k** | 3A/3B 验收：top_k 七种边界全 422；top_k=2→2 条 |
| C5 | "我们保留接口返回的顺序；返回数量不得超过 top_k。超量或存在非法条目会判为契约错误，不会静默截断后继续评分" | 返回按 score 降序、数量 ≤top_k；无非法条目 | 探针 + 测试 |
| C6 | Search 记录 "缺少 id / content，当前阶段都会立即失败" | 每条含 `id`+`content`（另有 `score`、`created_at`） | 公网响应体 |
| C7 | "Search 不得直接生成最终答案，也不得把答案伪装为记忆记录" | 只返回记忆证据原文 | 材料 §1 边界声明 |
| C8 | "现行规范不发送 filters、rerank 或 keyword_search" | 不依赖这些字段 | 接口实现 |

## D. 鉴权与健康检查

| # | 规范要求 | 我们的实现 | 证据 |
|---|---|---|---|
| D1 | "Add / Search 支持 Token、Bearer 和 X-Api-Key" | 三种全支持 | 三种方式实测（各 200） |
| D2 | "none 仅用于公开 smoke" | 生产端点**不开放无鉴权**：无密钥 → 401 | 实测 401 |
| D3 | "Health 接口通过无需鉴权的 GET 请求调用，返回任意 2xx 即正常" | `/health` 免鉴权、2xx；1 ms 级返回（内存快照） | 假 ollama 场景 5/5 = 200 |

## E. 错误处理与重试语义

| # | 规范要求 | 我们的实现 | 证据 |
|---|---|---|---|
| E1 | "平台业务错误通常采用 `{"detail":{"reason":"..."}}` 格式" | 5xx 均用该格式（如 `{"detail":{"reason":"storage backend unavailable..."}}`） | 假 ollama 探针输出 |
| E2 | "400/422 接口格式错误…不自动重试" | 格式/语义错误返 400/422 | 3A 用例 |
| E3 | "500/502/503/504 临时服务异常…自动退避重试" | 我们的 503（准入/预算/嵌入不可用/存储忙）与 5xx 都落在规范的可重试集合 | 背压实测 2×200+2×503；计数 `backpressure_503` |
| E4 | "Add 可保持同一 request_id 与请求体进行有界重试，但不能仅凭 409 视为成功" | 同 request_id 同 body 重试 → 200 replay（不是 409）；不同 body → 409 | 实测 |
| E5 | "重试和断点续跑保持同一 Add 的 request_id 与请求体" | 幂等表 + 写阶段检查点；P0.5 修复"resume 已全部完成时应直接 replay 200" | P0.5 任务书第 2 条 |

## F. 材料与合规承诺

| # | 规范要求 | 我们的状态 |
|---|---|---|
| F1 | 提交材料含 "系统名称与版本、联系人、团队、拟参评类型、方法说明、允许公开展示的信息、完整提交说明" | `AML-COMPETITION.md` / `.en.md` 已按此结构改写；**待填：联系人/团队、固定 commit** |
| F2 | "已部署的 Add/Search API、鉴权与容量声明，以及公开仓库、固定 commit、原始工作引用和方法改动说明" | 全部具备：容量 §2.2（实测）、两个存储层补丁披露 §4.3、改动表 §4.2 |
| F3 | "选择 full 前，必须逐项勾选提交清单：smoke 已通过、API 契约正确、运行说明完整、原创性披露与诚信承诺" | 待 smoke 通过后逐项确认 |
| F4 | "Open-source Methods entries must use gpt-4o-mini during both Add and Search"（Full Evaluation Gate 清单项） | **我们不在 Add/Search 中调用任何 LLM**（0 次外部模型调用）→ 无论该条指"平台评测侧模型"还是"限制参赛方法所用模型"，我们都满足；⚠️ 若其被解释为"参赛系统必须调用 gpt-4o-mini"，则与自托管/无外部依赖冲突 → **建议向主办方书面确认**（见 G1） |
| F5 | "不得跨 user_id、任务、样本或团队共享和检索评测记忆" | 硬隔离（author_id 过滤） |
| F6 | "评测数据及其派生副本只能用于完成当前任务…在任务完成后 30 天内删除相关数据" | 评测数据仅落本机独立目录；赛后按此期限**删除**（不只是归档）——已列入赛后处置清单 |

## G. 待确认 / 待办（提交前必须清零）

- **G1**：F4 的 gpt-4o-mini 条款解释（建议邮件确认；我们两种解释都合规，但要留书面依据）。
- **G2**：联系人/团队随**准入申请表**提交给主办方（公开仓库不展示个人姓名/邮箱）；**固定 commit 以部署端点 `/health` 返回值为准**（2026-09-19 晚实测 = `25a7364`，须**推送公开仓库后**才对评委可见）；提交前再抓一次官网页面比对（规范可能更新）。
- **G3**：smoke → Full #1 排期（建议 9 月底前 Full）。
- **G4**：评测数据在任务结束后 30 天内删除（赛后处置清单第 1 项）。
