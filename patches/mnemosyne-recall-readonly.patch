diff -ruN -x __pycache__ a/mnemosyne/core/beam.py b/mnemosyne/core/beam.py
--- a/mnemosyne/core/beam.py	2026-09-11 23:24:39.000000000 +0800
+++ b/mnemosyne/core/beam.py	2026-09-19 01:14:12.000000000 +0800
@@ -5463,6 +5463,7 @@
                fts_weight: float = None,
                importance_weight: float = None,
                explain: bool = False,
+               bump_recalled: bool = True,
                _cross_session: Optional[bool] = None) -> List[Dict]:
         """
         Hybrid recall across working_memory + episodic_memory.
@@ -5532,6 +5533,7 @@
                 channel_id=channel_id,
                 veracity=veracity, memory_type=memory_type,
                 cross_session=cross_session,
+                bump_recalled=bump_recalled,
             )
             if explain:
                 return {
@@ -6608,41 +6610,42 @@
         final_results = results[:top_k]
 
         # --- Recall tracking: increment counts + set last_recalled ---
-        now_iso = datetime.now().isoformat()
-        wm_ids = [r["id"] for r in final_results if r.get("tier") == "working"]
-        em_ids = [r["id"] for r in final_results if r.get("tier") == "episodic"]
-        cursor = self.conn.cursor()
-        if channel_id:
-            rec_scope = _session_scope_filter("channel_id", cross_session=cross_session)
-        elif author_id or author_type:
-            rec_scope = "(1=1)"
-        else:
-            rec_scope = _session_scope_filter(cross_session=cross_session)
-        if wm_ids:
-            placeholders = ",".join("?" * len(wm_ids))
-            rec_params = [now_iso, *tuple(wm_ids)]
-            if channel_id:
-                rec_params.extend(_session_scope_params(self.session_id, channel_id, cross_session=cross_session))
-            elif not (author_id or author_type):
-                rec_params.extend(_session_scope_params(self.session_id, cross_session=cross_session))
-            cursor.execute(f"""
-                UPDATE working_memory
-                SET recall_count = recall_count + 1, last_recalled = ?
-                WHERE id IN ({placeholders}) AND {rec_scope}
-            """, (*rec_params,))
-        if em_ids:
-            placeholders = ",".join("?" * len(em_ids))
-            rec_params = [now_iso, *tuple(em_ids)]
+        if bump_recalled:
+            now_iso = datetime.now().isoformat()
+            wm_ids = [r["id"] for r in final_results if r.get("tier") == "working"]
+            em_ids = [r["id"] for r in final_results if r.get("tier") == "episodic"]
+            cursor = self.conn.cursor()
             if channel_id:
-                rec_params.extend(_session_scope_params(self.session_id, channel_id, cross_session=cross_session))
-            elif not (author_id or author_type):
-                rec_params.extend(_session_scope_params(self.session_id, cross_session=cross_session))
-            cursor.execute(f"""
-                UPDATE episodic_memory
-                SET recall_count = recall_count + 1, last_recalled = ?
-                WHERE id IN ({placeholders}) AND {rec_scope}
-            """, (*rec_params,))
-        self.conn.commit()
+                rec_scope = _session_scope_filter("channel_id", cross_session=cross_session)
+            elif author_id or author_type:
+                rec_scope = "(1=1)"
+            else:
+                rec_scope = _session_scope_filter(cross_session=cross_session)
+            if wm_ids:
+                placeholders = ",".join("?" * len(wm_ids))
+                rec_params = [now_iso, *tuple(wm_ids)]
+                if channel_id:
+                    rec_params.extend(_session_scope_params(self.session_id, channel_id, cross_session=cross_session))
+                elif not (author_id or author_type):
+                    rec_params.extend(_session_scope_params(self.session_id, cross_session=cross_session))
+                cursor.execute(f"""
+                    UPDATE working_memory
+                    SET recall_count = recall_count + 1, last_recalled = ?
+                    WHERE id IN ({placeholders}) AND {rec_scope}
+                """, (*rec_params,))
+            if em_ids:
+                placeholders = ",".join("?" * len(em_ids))
+                rec_params = [now_iso, *tuple(em_ids)]
+                if channel_id:
+                    rec_params.extend(_session_scope_params(self.session_id, channel_id, cross_session=cross_session))
+                elif not (author_id or author_type):
+                    rec_params.extend(_session_scope_params(self.session_id, cross_session=cross_session))
+                cursor.execute(f"""
+                    UPDATE episodic_memory
+                    SET recall_count = recall_count + 1, last_recalled = ?
+                    WHERE id IN ({placeholders}) AND {rec_scope}
+                """, (*rec_params,))
+            self.conn.commit()
 
         # [C4] Final tier-attribution records. Each counter holds the
         # number of kept rows attributed to that tier on this call.
@@ -7243,7 +7246,8 @@
                            channel_id: Optional[str] = None,
                            veracity: Optional[str] = None,
                            memory_type: Optional[str] = None,
-                           cross_session: Optional[bool] = None) -> List[Dict]:
+                           cross_session: Optional[bool] = None,
+                           bump_recalled: bool = True) -> List[Dict]:
         """[E5] Polyphonic recall path.
 
         Delegates to PolyphonicRecallEngine when MNEMOSYNE_POLYPHONIC_RECALL=1.
@@ -7386,7 +7390,7 @@
         # the linear path updates them and downstream features (decay
         # scheduling, importance reinforcement) depend on the signal.
         # /review caught the missing update as a silent telemetry loss.
-        if recalled_episodic_ids:
+        if bump_recalled and recalled_episodic_ids:
             placeholders = ",".join("?" * len(recalled_episodic_ids))
             params = [now_iso, *recalled_episodic_ids, *_rec_scope_params()]
             self.conn.execute(
@@ -7394,7 +7398,7 @@
                 f"last_recalled = ? WHERE id IN ({placeholders}) AND {rec_scope}",
                 tuple(params),
             )
-        if recalled_working_ids:
+        if bump_recalled and recalled_working_ids:
             placeholders = ",".join("?" * len(recalled_working_ids))
             params = [now_iso, *recalled_working_ids, *_rec_scope_params()]
             self.conn.execute(
@@ -7402,7 +7406,7 @@
                 f"last_recalled = ? WHERE id IN ({placeholders}) AND {rec_scope}",
                 tuple(params),
             )
-        if recalled_episodic_ids or recalled_working_ids:
+        if bump_recalled and (recalled_episodic_ids or recalled_working_ids):
             self.conn.commit()
 
         # --- MEMORIA structured fact supplement (polyphonic path) ---
diff -ruN -x __pycache__ a/mnemosyne/core/memory.py b/mnemosyne/core/memory.py
--- a/mnemosyne/core/memory.py	2026-09-11 23:24:39.000000000 +0800
+++ b/mnemosyne/core/memory.py	2026-09-19 01:13:56.000000000 +0800
@@ -431,7 +431,8 @@
                vec_weight: float = None,
                fts_weight: float = None,
                importance_weight: float = None,
-               explain: bool = False) -> List[Dict]:
+               explain: bool = False,
+               bump_recalled: bool = True) -> List[Dict]:
         """
         Search memories with hybrid relevance scoring.
         Uses BEAM episodic + working memory retrieval (sqlite-vec + FTS5).
@@ -439,6 +440,8 @@
         Supports multi-agent identity filtering: author_id, author_type, channel_id.
         Supports temporal scoring: temporal_weight, query_time, temporal_halflife.
         Supports scoring weight overrides: vec_weight, fts_weight, importance_weight.
+        bump_recalled=False makes recall read-only (no recall_count/last_recalled
+        write); the default True preserves the historical write-back behavior.
         """
         import os as _os
         if _os.environ.get("MNEMOSYNE_ENHANCED_RECALL", "0") == "1":
@@ -453,7 +456,8 @@
                                              vec_weight=vec_weight,
                                              fts_weight=fts_weight,
                                              importance_weight=importance_weight,
-                                             explain=explain)
+                                             explain=explain,
+                                             bump_recalled=bump_recalled)
         return self.beam.recall(query, top_k=top_k,
                                 from_date=from_date, to_date=to_date,
                                 source=source, topic=topic,
@@ -465,7 +469,8 @@
                                 vec_weight=vec_weight,
                                 fts_weight=fts_weight,
                                 importance_weight=importance_weight,
-                                explain=explain)
+                                explain=explain,
+                                bump_recalled=bump_recalled)
 
     def _emit_wrapper(self, event_type: str, memory_id: str, **kwargs) -> None:
         """Emit a streaming event through the Mnemosyne wrapper layer."""
@@ -987,7 +992,8 @@
            fts_weight: float = None,
            importance_weight: float = None,
            explain: bool = False,
-           bank: str = None) -> List[Dict]:
+           bank: str = None,
+           bump_recalled: bool = True) -> List[Dict]:
     """Search memories using the global instance with temporal filtering and scoring"""
     return _get_default(bank).recall(query, top_k,
                                      from_date=from_date, to_date=to_date,
@@ -998,7 +1004,8 @@
                                      vec_weight=vec_weight,
                                      fts_weight=fts_weight,
                                      importance_weight=importance_weight,
-                                     explain=explain)
+                                     explain=explain,
+                                     bump_recalled=bump_recalled)
 
 
 def get_context(limit: int = 10, bank: str = None) -> List[Dict]:
